using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CavalryMovement : Movement{
    // animator events.
    //public void back_strike_forward_leap()  => dash(transform.rotation.y == 0? Vector2.right : Vector2.left, 20, 0.75f);
    //public void back_strike_backward_jump() => dash(transform.rotation.y == 0? Vector2.left : Vector2.right, 20, 0.6f);
    //public void jump_away_dash()            => dash(transform.rotation.y == 0? Vector2.left : Vector2.right, 15, 0.55f);
    //public void bite_lunge()                => dash(transform.rotation.y == 0? Vector2.right : Vector2.left, 20, 0.2f);
}
